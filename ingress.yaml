apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: trio-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: trio.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: trio-service
                port:
                  number: 80
