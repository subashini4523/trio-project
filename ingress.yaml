apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jenifer-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: jenifer.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: jenifer-service
                port:
                  number: 80
